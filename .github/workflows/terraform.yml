name: Terraform

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - prod
      action:
        description: 'Action'
        required: true
        default: 'plan'
        type: choice
        options:
          - validate
          - plan
          - apply

env:
  DEFAULT_ENVIRONMENT: 'dev'
  DEFAULT_ACTION: 'plan'

jobs:
  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./env/${{ github.event.inputs.environment || env.DEFAULT_ENVIRONMENT }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: '1.8.0'
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: 'us-east-1'
      
      - name: Terraform Init
        run: terraform init
      
      - name: Terraform Validate
        if: ${{ (github.event.inputs.action || env.DEFAULT_ACTION) == 'validate' || (github.event.inputs.action || env.DEFAULT_ACTION) == 'plan' || (github.event.inputs.action || env.DEFAULT_ACTION) == 'apply' }}
        run: terraform validate
      
      - name: Terraform Plan
        if: ${{ (github.event.inputs.action || env.DEFAULT_ACTION) == 'plan' || (github.event.inputs.action || env.DEFAULT_ACTION) == 'apply' }}
        run: terraform plan
      
      - name: Terraform Apply
        if: ${{ (github.event.inputs.action || env.DEFAULT_ACTION) == 'apply' }}
        run: terraform apply -auto-approve
